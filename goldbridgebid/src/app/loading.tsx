import RouteLoadingState from "@/components/loading/RouteLoadingState";

export default function Loading() {
  return (
    <RouteLoadingState
      title="Loading ProjectXBidX"
      message="One moment while we load the next page and get everything ready."
    />
  );
}
